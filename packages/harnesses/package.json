{
  "name": "@engram/harnesses",
  "version": "0.2.0",
  "private": true,
  "description": "Harness integrations for engram: a shared operations layer over @engram/core plus per-harness adapters (Pi coding-agent extension, Claude Code plugin).",
  "type": "module",
  "exports": {
    "./shared": "./src/shared/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json"
  },
  "dependencies": {
    "effect": "catalog:"
  },
  "devDependencies": {
    "@effect/platform-node": "catalog:",
    "@effect/vitest": "catalog:",
    "@engram/core": "workspace:*"
  }
}
